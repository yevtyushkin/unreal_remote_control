import 'package:flutter/material.dart';

/// A widget that displays the tuning tab of a project page split view.
class ProjectPagePresetSplitViewTabTuning extends StatelessWidget {
  /// Returns a new instance of the [ProjectPagePresetSplitViewTabTuning].
  const ProjectPagePresetSplitViewTabTuning({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
